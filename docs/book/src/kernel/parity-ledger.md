# The parity ledger

The committed audit, included verbatim from
[`docs/PARITY.md`](https://github.com/gilbertorconde/ogeom-rs/blob/main/docs/PARITY.md).
It is generated from the reference index and the hand-written ledger, and
`tools/check.sh` fails if it goes stale — so this chapter is as current
as the commit the book was built from.

---

{{#include ../../../PARITY.md}}
