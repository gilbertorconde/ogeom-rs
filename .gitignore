/target
**/*.rs.bk
Cargo.lock.orig

# difftest / ogeom-view build artifacts
/tools/difftest/target
/tools/ogeom-view/target

# Reference material a developer may keep locally. Never committed: the kernel
# has no dependency on any existing CAD kernel, and nothing in this repo pulls
# one in. See CONTRIBUTING.md.
/vendor/
/reference/

# Generated analysis artifacts. Derived from other projects' sources and
# regenerable on demand; see docs/SCOPE.md.
/docs/api_surface.json
/docs/*.json.tmp

.DS_Store
*.swp

*.proptest-regressions
